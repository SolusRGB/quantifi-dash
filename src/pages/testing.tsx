import HomeTableTest from "@/components/tables/testing/HomeTableTest";

/**
 * This is the testing page for the application.
 * This was created to test the functionality of adding projects to the database
 * Then routing it into the front end of the application.
 * @returns {JSX.Element} - The testing page.
 */

export default function testing() {
  return (
    <div>
      testing..
      <HomeTableTest />;
    </div>
  );
}
